<div class="row justify-content-center">
  <div class="col-lg-5">
    <div class="card border-0 shadow-sm">
      <div class="card-body p-4">
        <h1 class="h4 fw-bold mb-4">Change your password</h1>

        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
          <%= f.hidden_field :reset_password_token %>

          <div class="mb-3">
            <%= f.label :password, "New password", class: "form-label fw-semibold" %>
            <% if @minimum_password_length %>
              <small class="text-muted ms-1">(<%= @minimum_password_length %> characters minimum)</small>
            <% end %>
            <%= f.password_field :password, class: "form-control #{'is-invalid' if resource.errors[:password].any?}", required: true, minlength: @minimum_password_length, maxlength: 128, autofocus: true, autocomplete: "new-password" %>
            <div class="invalid-feedback"><%= resource.errors[:password].first %></div>
          </div>

          <div class="mb-4">
            <%= f.label :password_confirmation, "Confirm new password", class: "form-label fw-semibold" %>
            <%= f.password_field :password_confirmation, class: "form-control #{'is-invalid' if resource.errors[:password_confirmation].any?}", required: true, maxlength: 128, autocomplete: "new-password" %>
            <div class="invalid-feedback"><%= resource.errors[:password_confirmation].first %></div>
          </div>

          <%= f.submit "Change my password", class: "btn btn-primary w-100" %>
        <% end %>

        <div class="mt-3 text-center text-muted small">
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>

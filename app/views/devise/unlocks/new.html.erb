<div class="row justify-content-center">
  <div class="col-lg-5">
    <div class="card border-0 shadow-sm">
      <div class="card-body p-4">
        <h1 class="h4 fw-bold mb-4">Resend unlock instructions</h1>

        <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
          <div class="mb-4">
            <%= f.label :email, class: "form-label fw-semibold" %>
            <%= f.email_field :email, class: "form-control #{'is-invalid' if resource.errors[:email].any?}", autofocus: true, autocomplete: "email", required: true, maxlength: 255 %>
            <div class="invalid-feedback"><%= resource.errors[:email].first %></div>
          </div>

          <%= f.submit "Resend unlock instructions", class: "btn btn-primary w-100" %>
        <% end %>

        <div class="mt-3 text-center text-muted small">
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>
